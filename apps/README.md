# Apps

This directory contains deployable application entrypoints.

- `mobile-ios` is the React Native client.
- `api-gateway` is the external API entrypoint.
- `services/*` contains backend microservice entrypoints.
